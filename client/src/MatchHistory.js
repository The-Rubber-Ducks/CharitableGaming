import MatchList from "./MatchList";

const MatchHistory = ({ gameTitle }) => {
    return (  
        <div className="match-history-container">
            <MatchList />
        </div>
    );
}
 
export default MatchHistory;